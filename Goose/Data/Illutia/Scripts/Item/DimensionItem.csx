using System;
using Goose;
using Goose.Scripting;

/// <summary>The per-item dimension script: rolls the abyss suffixes and rarity titles onto
/// dimension equipment. Filled in by Task 6; this stub exists so the script harness can
/// install and compile it from Task 5 onward.</summary>
public class DimensionItem : BaseItemScript
{
}

return typeof(DimensionItem);
